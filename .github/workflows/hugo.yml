name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.163.3
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.163.3'
          extended: true
      - name: Get Hugo Modules
        run: |
          cd website && hugo mod get
      - name: Build with Hugo

Ich sehe der neueste Workflow ist wieder fehlgeschlagen. Lass mich die Fehlermeldung sehen:

1. Klick auf den neuesten Workflow ("Update hugo.yml" - ganz oben)
2. Klick auf den fehlgeschlagenen Job
3. Klick auf "Set up Hugo" oder "Build with Hugo" (welcher auch rot ist)
4. Schick mir einen Screenshot der Fehlermeldung

Oder noch besser - editiere den Workflow direkt:

1. Klick auf den neuesten Workflow
2. Klick auf das Stift-Icon (Edit this file) oben rechts
3. Ersetze den gesamten Inhalt mit diesem Code:

```yaml
name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.163.3
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.163.3'
          extended: true
      - name: Get Hugo Modules
        run: |
          cd website && hugo mod get
      - name: Build with H
